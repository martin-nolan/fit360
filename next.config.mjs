/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options go here.
  // For example, to enable the `app` directory:
  // experimental: {
  //   appDir: true,
  // },
  // Note: appDir is enabled by default in Next.js 13.4+
};

export default nextConfig;
